RewriteEngine On

# Authorizationヘッダーを環境変数として渡す
RewriteCond %{HTTP:Authorization} .
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

# /mcp/messages を messages.php にリライト
RewriteRule ^messages$ messages.php [L]
